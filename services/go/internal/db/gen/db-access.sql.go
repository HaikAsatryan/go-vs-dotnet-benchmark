// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: db-access.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const customerExists = `-- name: CustomerExists :one
SELECT EXISTS(SELECT 1 FROM customers WHERE id = $1) AS exists
`

func (q *Queries) CustomerExists(ctx context.Context, id int64) (bool, error) {
	row := q.db.QueryRow(ctx, customerExists, id)
	var exists bool
	err := row.Scan(&exists)
	return exists, err
}

const getInvoice = `-- name: GetInvoice :one

SELECT id, customer_id, status, currency, total_minor, created_at
FROM invoices
WHERE id = $1
`

// spec/db-access.sql — canonical SQL, single source of truth (spec/db-access.md).
// sqlc consumes this file verbatim (annotations below). The .NET raw ADO.NET variant
// uses these statement bodies literally. EF LINQ must produce the same shape
// (asserted by capturing EF-generated SQL).
func (q *Queries) GetInvoice(ctx context.Context, id int64) (Invoice, error) {
	row := q.db.QueryRow(ctx, getInvoice, id)
	var i Invoice
	err := row.Scan(
		&i.ID,
		&i.CustomerID,
		&i.Status,
		&i.Currency,
		&i.TotalMinor,
		&i.CreatedAt,
	)
	return i, err
}

const getInvoiceItems = `-- name: GetInvoiceItems :many
SELECT id, sku, description, qty, unit_price_minor, line_total_minor
FROM invoice_items
WHERE invoice_id = $1
ORDER BY id
`

type GetInvoiceItemsRow struct {
	ID             int64
	Sku            string
	Description    string
	Qty            int32
	UnitPriceMinor int64
	LineTotalMinor int64
}

func (q *Queries) GetInvoiceItems(ctx context.Context, invoiceID int64) ([]GetInvoiceItemsRow, error) {
	rows, err := q.db.Query(ctx, getInvoiceItems, invoiceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetInvoiceItemsRow
	for rows.Next() {
		var i GetInvoiceItemsRow
		if err := rows.Scan(
			&i.ID,
			&i.Sku,
			&i.Description,
			&i.Qty,
			&i.UnitPriceMinor,
			&i.LineTotalMinor,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const insertInvoice = `-- name: InsertInvoice :one
INSERT INTO invoices (customer_id, status, currency, total_minor)
VALUES ($1, $2, $3, $4)
RETURNING id, created_at
`

type InsertInvoiceParams struct {
	CustomerID int64
	Status     string
	Currency   string
	TotalMinor int64
}

type InsertInvoiceRow struct {
	ID        int64
	CreatedAt pgtype.Timestamptz
}

func (q *Queries) InsertInvoice(ctx context.Context, arg InsertInvoiceParams) (InsertInvoiceRow, error) {
	row := q.db.QueryRow(ctx, insertInvoice,
		arg.CustomerID,
		arg.Status,
		arg.Currency,
		arg.TotalMinor,
	)
	var i InsertInvoiceRow
	err := row.Scan(&i.ID, &i.CreatedAt)
	return i, err
}

const insertInvoiceItem = `-- name: InsertInvoiceItem :one
INSERT INTO invoice_items (invoice_id, sku, description, qty, unit_price_minor, line_total_minor)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id
`

type InsertInvoiceItemParams struct {
	InvoiceID      int64
	Sku            string
	Description    string
	Qty            int32
	UnitPriceMinor int64
	LineTotalMinor int64
}

func (q *Queries) InsertInvoiceItem(ctx context.Context, arg InsertInvoiceItemParams) (int64, error) {
	row := q.db.QueryRow(ctx, insertInvoiceItem,
		arg.InvoiceID,
		arg.Sku,
		arg.Description,
		arg.Qty,
		arg.UnitPriceMinor,
		arg.LineTotalMinor,
	)
	var id int64
	err := row.Scan(&id)
	return id, err
}

const listInvoicesByCustomer = `-- name: ListInvoicesByCustomer :many
SELECT id, customer_id, status, currency, total_minor, created_at
FROM invoices
WHERE customer_id = $1
ORDER BY id DESC
LIMIT 20 OFFSET $2
`

type ListInvoicesByCustomerParams struct {
	CustomerID int64
	Offset     int32
}

func (q *Queries) ListInvoicesByCustomer(ctx context.Context, arg ListInvoicesByCustomerParams) ([]Invoice, error) {
	rows, err := q.db.Query(ctx, listInvoicesByCustomer, arg.CustomerID, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Invoice
	for rows.Next() {
		var i Invoice
		if err := rows.Scan(
			&i.ID,
			&i.CustomerID,
			&i.Status,
			&i.Currency,
			&i.TotalMinor,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateCustomerBalance = `-- name: UpdateCustomerBalance :exec
UPDATE customers
SET balance_minor = balance_minor + $2
WHERE id = $1
`

type UpdateCustomerBalanceParams struct {
	ID           int64
	BalanceMinor int64
}

func (q *Queries) UpdateCustomerBalance(ctx context.Context, arg UpdateCustomerBalanceParams) error {
	_, err := q.db.Exec(ctx, updateCustomerBalance, arg.ID, arg.BalanceMinor)
	return err
}
